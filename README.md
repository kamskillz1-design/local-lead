# LocalLead CRM

Vite + React SPA. Data and auth: Supabase. Hosting: Vercel.

```bash
npm install
cp .env.example .env.local
npm run dev
```

See `DEPLOY.md` for schema, Auth URLs, Storage bucket `crm-uploads`, and Edge Functions.
